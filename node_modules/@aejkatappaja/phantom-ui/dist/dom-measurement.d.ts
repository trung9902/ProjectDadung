/**
 * Core DOM measurement engine.
 * Walks the slotted DOM tree and extracts leaf element positions/sizes
 * to generate shimmer overlay blocks.
 *
 * Based on a well-established technique (getBoundingClientRect overlay skeletons)
 * with prior art from page-skeleton-webpack-plugin (2018)
 * and @findify/skeleton-generator (~2019).
 */
interface BaseInfo {
    x: number;
    y: number;
    width: number;
    height: number;
    borderRadius: string;
}
export interface ElementInfo extends BaseInfo {
    isContainer?: boolean;
    containerBg?: string;
    containerBorder?: string;
    containerShadow?: string;
}
export interface ContainerInfo extends BaseInfo {
    backgroundColor: string;
    border: string;
    boxShadow: string;
}
export declare function extractElementInfo(element: Element, parentRect: DOMRect): ElementInfo[];
export declare function extractContainerInfo(element: Element, parentRect: DOMRect): ContainerInfo | null;
export declare function createResizeObserver(element: Element, callback: () => void): ResizeObserver;
export {};
